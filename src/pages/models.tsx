import { Layout } from '~/components/Layout';
import { ModelList } from '~/components/ModelList';

const Models = () => {
  return (
    <Layout>
      <ModelList />
    </Layout>
  );
};

export default Models;
